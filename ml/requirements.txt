scikit-learn>=1.5
pyarrow>=17.0
duckdb>=1.1
# pandas 3.0 is a breaking release
pandas>=2.2,<3
# duckdb refuses to read or write its progress-bar setting under a Jupyter
# kernel without this - required, not cosmetic
ipywidgets>=8.1
matplotlib>=3.9
joblib>=1.4
# keep the major version aligned with Dockerfile.mlflow
mlflow>=3.1,<4
