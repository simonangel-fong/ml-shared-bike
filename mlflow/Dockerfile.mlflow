# MLflow tracking server. The official image ships without a Postgres driver,
# so the backend store URI would fail to resolve on startup.
FROM ghcr.io/mlflow/mlflow:v3.14.0

RUN pip install --no-cache-dir psycopg2-binary==2.9.10

EXPOSE 5000
